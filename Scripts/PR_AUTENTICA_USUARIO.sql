DROP PROCEDURE IF EXISTS PR_AUTENTICA_USUARIO
GO

CREATE PROCEDURE PR_AUTENTICA_USUARIO
(
	@Login VARCHAR(15),
	@Senha VARCHAR(100)
)
AS
BEGIN

	SELECT
		*
	FROM
		tbUsuario
	WHERE
		Login = @Login
		AND Senha = @Senha

END
GO